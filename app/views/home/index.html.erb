<h1>Welcome <%= current_user.email%></h1>
<br>
<h2>Here are the hikes you have created</h2>
 <%current_user.hikes.order("created_at DESC").each do |hike|%>
            <div class="col-lg-4 col-sm-6 mb-4">
                <div class="card h-100">
                    <img class="card-img-top, width: 700px, height: 400px" src="<%=hike.img_url%>" alt=""></a>
                    <div class="card-body">
                        <h5 class="card-title"><%=hike.name%></h5>
                    </div>
                    <a class="btn btn-info" href="/hikes/<%=hike.id%>">Show Hike</a>
                </div>
            </div>
<%end%>

            <%
=begin%>
 <h3>Here are your reivews:</h3>
            <%current_user.reviews.order("created_at DESC").each do |review|%>
            <div class="col-lg-4 col-sm-6 mb-4">
                <div class="card h-100">
                    <div class="card-body">
                        <h5 class="card-title"><%=review.content%></h5>
                    </div>
                    <%= link_to  "Write Your Review",  hike_review_path(@hike) %>
                </div>
            </div> 
<%
=end%>

